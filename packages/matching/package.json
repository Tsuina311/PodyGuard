{
  "name": "@podyguard/matching",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "simulate": "tsx src/cli-simulate.ts",
    "benchmark": "tsx src/cli-benchmark.ts",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@podyguard/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "fast-check": "^4.1.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
