{
  "name": "poderate",
  "private": true,
  "packageManager": "yarn@3.8.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "yarn workspace @poderate/web dev",
    "dev:server": "yarn workspace @poderate/server dev",
    "build": "yarn workspace @poderate/shared build && yarn workspace @poderate/server build && yarn workspace @poderate/web build",
    "typecheck": "yarn workspace @poderate/shared typecheck && yarn workspace @poderate/server typecheck && yarn workspace @poderate/web typecheck",
    "test": "yarn workspace @poderate/shared test && yarn workspace @poderate/server test && yarn workspace @poderate/web test",
    "db:generate": "yarn workspace @poderate/server db:generate",
    "db:migrate": "yarn workspace @poderate/server db:migrate"
  }
}
